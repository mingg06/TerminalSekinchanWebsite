
  # Theme Park Website

  This is a code bundle for Theme Park Website. The original project is available at https://www.figma.com/design/F4fcDbgneI2CLGlbiSVJud/Theme-Park-Website.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  